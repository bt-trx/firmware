matrix:
  include:
    - language: cpp
      dist: xenial
      script:
        - ./test/build.sh
    - language: python
      python: 2.7
      cache:
        directories:
          - "~/.platformio"
      install:
        - pip install -U platformio
      script:
        - platformio run -v